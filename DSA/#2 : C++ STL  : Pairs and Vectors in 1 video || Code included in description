#include <bits/stdc++.h>
using namespace std;
int main (){
/*

	pair <int, int > p = {1,3};
    cout << p.first << " " << p.second;
    pair <int, pair<int,int>> q = {1, {2,3}};
    cout << q.second.second;
	
	vector<int> v;
	v.push_back(1);
	v.emplace_back(2);
	v.push_back(3);
	v.push_back(5);


	vector<pair<int,int>> vec;
	vec.push_back({1,2});
	vec.emplace_back(1,2);

	vector<int>v1(5,100);

	for (auto it : v1) cout << it << endl;

	vector<int>v2(v1);

	
	vector<int>:: iterator it = v.begin();
	//cout << *(it) << endl;
	//cout <<v[0] << endl;

	//cout << v.back() << endl;

	//for (auto it : v) cout << it << " ";
	//cout << endl;

	v.erase(v.begin()+1,v.begin()+3);

	//for (auto it : v) cout << it << " ";
	//cout << endl;

	v.insert(v.begin()+1,2,6020);
	//for (auto it : v) cout << it << " ";
	//cout << endl;
	
	//cout << v.size() << endl;
	v.pop_back();
	for (auto it : v) cout << it << " ";
	cout << endl;

	cout << v.empty()<< endl;
	
	*/
}
